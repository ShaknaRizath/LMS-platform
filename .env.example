# Local dev database managed by `npx prisma dev` (see prisma.config.ts). Swap for a real
# managed Postgres (Neon/Supabase/RDS) connection string for staging/production.
DATABASE_URL="postgresql://user:password@localhost:5432/cims_campus?schema=public"

# Auth.js
AUTH_SECRET="generate-with-npx-auth-secret"
AUTH_URL="http://localhost:3000"

# Google sign-in — create an OAuth client at https://console.cloud.google.com/apis/credentials
# and register this exact redirect URI: http://localhost:3000/api/auth/callback/google
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# File storage (Cloudinary) — leave blank in dev to no-op; fill in to enable real uploads
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
STORAGE_PROVIDER="cloudinary"

# Email (Resend) — leave RESEND_API_KEY blank to use the console/dev notification adapter
RESEND_API_KEY=""
EMAIL_FROM="CIMS Campus <no-reply@cims.edu>"

# Web push — generate once with `npx web-push generate-vapid-keys`. NEXT_PUBLIC_VAPID_PUBLIC_KEY
# must be the same value as VAPID_PUBLIC_KEY (exposed client-side to subscribe the browser).
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:admin@cims.edu"
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
